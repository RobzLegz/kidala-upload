import React from 'react';
import Logo from '../Logo';

const HomeNav = () => {
    return (
        <nav className="w-full">
            <button>
                <Logo />
            </button>
        </nav>
    );
};

export default HomeNav;
